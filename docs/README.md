# Screenshots

Images referenced by the top-level `README.md`. Drop PNGs here with these
exact names:

| File                 | What to capture                                                                                    |
| -------------------- | -------------------------------------------------------------------------------------------------- |
| `status-bar.png`     | A tmux status bar with attention icons — e.g. 🟠/🔥/⚙️ in the window list and a summary in `status-left`, ideally across a couple of sessions. |
| `session-picker.png` | The `prefix + a` popup: sessions with icons in the left gutter, one expanded, the two-line header (dimmed hotkeys, then view/sort state) visible. |
| `new-session.png`    | The directory picker (`prefix + A` or `ctrl-n`): a fuzzy query typed in, a match highlighted, the `new session >` prompt and `enter: create/switch | esc: back` header. |
